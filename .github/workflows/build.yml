name: build

on:
  push:
    branchs: [main]

jobs:
  build:
    runs-on: ubuntu-latest 

    steps:
      - uses: actions/checkout@v2
    
      - name: Install environment
        run: ./configure

      - name: Build
        run: make


